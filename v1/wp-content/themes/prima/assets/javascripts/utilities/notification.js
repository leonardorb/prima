// Generated by CoffeeScript 1.6.3
(function() {
  Prima.Utilities.Notification = (function() {
    function Notification() {
      this.overlay = $('.overlay');
      this.notification = $('.notification');
      this.notificationClassInfo = 'info';
      this.notificationClassError = 'error';
      this.setNotificationClass(this.notificationClassInfo);
      this.setEvents();
    }

    Notification.prototype.resetNotificationClass = function() {
      this.notification.removeClass(this.notificationClassInfo);
      return this.notification.removeClass(this.notificationClassError);
    };

    Notification.prototype.setNotificationClass = function(notificationClass) {
      return this.notification.addClass(notificationClass);
    };

    Notification.prototype.setEvents = function() {
      var _this = this;
      this.overlay.on('click', function() {
        return _this.hideNotification();
      });
      return $(document).keyup(function(ev) {
        if (ev.keyCode === 27) {
          return _this.hideNotification();
        }
      });
    };

    Notification.prototype.setNotification = function(type, content) {
      this.resetNotificationClass();
      if (type === 'info') {
        this.setNotificationClass(this.notificationClassInfo);
      }
      if (type === 'error') {
        this.setNotificationClass(this.notificationClassError);
      }
      return this.notification.html(content);
    };

    Notification.prototype.showNotification = function() {
      this.overlay.fadeIn(200);
      return this.notification.slideDown(200);
    };

    Notification.prototype.hideNotification = function() {
      this.overlay.fadeOut(200);
      return this.notification.fadeOut(200);
    };

    return Notification;

  })();

}).call(this);
